# Credenciales y secretos — son llaves de acceso a tus servicios externos (Google,
# OpenAI, etc.). No son decisión del participante porque son secretos del proveedor.
credenciales.env
.env
*.env.local
*.env.production
client_secret_*.json
token.json
*.pickle

# Sistema operativo y editores
.DS_Store
._*
Thumbs.db
desktop.ini
.vscode/
.idea/
*.swp
*.swo
*~

# Logs y cache
*.log
debug.log
npm-debug.log*
.cache/
__pycache__/
*.pyc
node_modules/
.next/
.turbo/

# El resto — documentos en memoria/, capturas en conversaciones/ y
# mi-resultado/capturas/, borradores, etc. — es DECISIÓN TUYA si lo publicas o no.
# Tu asistente IA te avisará si detecta algo que conviene revisar antes de publicar.
# Si prefieres ignorar algún archivo o carpeta en particular, agrégalo abajo.
